{
  "name": "vscode-pb-suite",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile:lang": "npm -w packages/pb-lang-support run compile",
    "compile:forms": "npm -w packages/pb-forms-editor run compile",
    "compile:all": "npm run compile:lang && npm run compile:forms"
  }
}
